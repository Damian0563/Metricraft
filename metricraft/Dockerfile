FROM node:20-alpine
WORKDIR /app

ARG SECRET
ARG DATABASE_USERS
ARG DATABASE_LOGS

ENV SECRET=$SECRET
ENV DATABASE_USERS=$DATABASE_USERS
ENV DATABASE_LOGS=$DATABASE_LOGS

COPY . ./
RUN npm ci
RUN npm run build

EXPOSE 8000

CMD ["node", ".output/server/index.mjs"]
