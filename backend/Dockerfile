FROM golang:1.26-alpine
WORKDIR /app

ARG SECRET
ARG DATABASE_USERS

ENV SECRET=$SECRET
ENV DATABASE_USERS=$DATABASE_USERS

COPY . ./
RUN go mod download
RUN go build -o backend

EXPOSE 8080

CMD ["./backend"]
