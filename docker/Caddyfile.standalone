# All-in-one container: UI, API, and worker proxy share one service name.
:80 {
	@api path /sign /dashboard/* /settings/* /verify/* /ws/*
	handle @api {
		reverse_proxy metricraft:8080
	}
	handle {
		reverse_proxy metricraft:8000
	}
}

:8081 {
	reverse_proxy metricraft:8081
}
